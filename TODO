TODO list

symbols explained :
v = done
. = to do
> = in progress
! = bug
x = canceled

New ideas are added on top of the list and are sometimes reordered.

... release version
... documentation - improve documentation quality
... define push strategy (gitlab+github)
>>> CI management
   vvv include default .gitlab-ci.yml
   ... include default .travis.yml
vvv verbose mode
vvv role_init - must refacto script
   vvv usage function
   vvv role_init can work as a link (installation easier)
   vvv check parameters
vvv have testing image instead of before_script
vvv organise tests in groups : images, library, role_init
